import Client from './client';

// metadata
export const metadata = {
  title: "RSC Wasquehal - Règles",
  description: "Les règles du sport",
}

export default function Index() {
  return (
    <Client />
  )
}