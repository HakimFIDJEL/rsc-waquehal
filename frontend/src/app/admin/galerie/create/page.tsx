import Client from './client';

// metadata
export const metadata = {
  title: "RSC Wasquehal - Galerie",
  description: "Créer une image dans la galerie",
}

export default function Index() {
  return (
    <Client />
  )
}